import React from 'react';
import IconButton from 'material-ui/IconButton';

const ContactBar = () => (
  <div>
    <IconButton
      iconClassName="github-icon" tooltip="bottom-center"
      tooltipPosition="bottom-center" 
    />
  </div>
);
export default ContactBar;